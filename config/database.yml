# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: hr_apply_onlinedev
  username: root
  password: Sudhir@99
  host: localhost
  timeout: 50000
  reconnect: true

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  timeout: 5000

production:
  adapter: mysql
  database: hr_apply_online
  username: root
  password: McMilk1
  host: localhost
  timeout: 50000
  reconnect: true
